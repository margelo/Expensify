diff --git a/node_modules/@legendapp/list/keyboard.d.ts b/node_modules/@legendapp/list/keyboard.d.ts
index 43678c2..759d16d 100644
--- a/node_modules/@legendapp/list/keyboard.d.ts
+++ b/node_modules/@legendapp/list/keyboard.d.ts
@@ -260,6 +260,10 @@ type LegendListRef = Omit<LegendListRef$1, "getAnimatableRef" | "getNativeScroll
 };
 
 type KeyboardChatScrollViewPropsUnique = Omit<KeyboardChatScrollViewProps, keyof ScrollViewProps | "inverted" | "ScrollViewComponent" | "blankSpace" | "extraContentPadding" | "onContentInsetChange" | "offset">;
+type KeyboardAwareScrollViewComponentProps = NonNullable<KeyboardChatScrollViewProps["ScrollViewComponent"]> extends React.ForwardRefExoticComponent<infer Props> ? Omit<Props, "ref"> & {
+    ref?: React.Ref<React.ElementRef<typeof ScrollViewComponent>>;
+} : never;
+type KeyboardAwareScrollViewComponent = (props: KeyboardAwareScrollViewComponentProps) => React.ReactElement | null;
 type ScrollMessageToEndOptions = {
     animated: boolean;
     closeKeyboard: boolean;
@@ -293,6 +297,7 @@ declare const KeyboardAwareLegendList: <ItemT>(props: Omit<AnimatedLegendListPro
     anchoredEndSpace?: AnchoredEndSpaceConfig;
     contentInsetEndAdjustment?: SharedValue<number>;
     keyboardOffset?: number;
+    ScrollViewComponent?: KeyboardAwareScrollViewComponent;
 } & React.RefAttributes<LegendListRef>) => React.ReactElement | null;
 
 export { KeyboardAwareLegendList, useKeyboardChatComposerInset, useKeyboardScrollToEnd };
diff --git a/node_modules/@legendapp/list/keyboard.js b/node_modules/@legendapp/list/keyboard.js
index 6645bcb..dda6e20 100644
--- a/node_modules/@legendapp/list/keyboard.js
+++ b/node_modules/@legendapp/list/keyboard.js
@@ -1,6 +1,7 @@
 'use strict';
 
 var React = require('react');
+var reactNative$1 = require('react-native');
 var reactNativeKeyboardController = require('react-native-keyboard-controller');
 var reactNativeReanimated = require('react-native-reanimated');
 var reactNative = require('@legendapp/list/react-native');
@@ -90,6 +91,7 @@ var KeyboardAwareLegendList = typedForwardRef(function KeyboardAwareLegendList2(
     freeze,
     keyboardLiftBehavior,
     keyboardOffset,
+    ScrollViewComponent,
     ...rest
   } = props;
   const refLegendList = React.useRef(null);
@@ -129,7 +131,8 @@ var KeyboardAwareLegendList = typedForwardRef(function KeyboardAwareLegendList2(
           freeze,
           keyboardLiftBehavior,
           offset: keyboardOffset,
-          onContentInsetChange
+          onContentInsetChange: reactNative$1.Platform.OS === "android" ? onContentInsetChange : void 0,
+          ScrollViewComponent
         }
       );
     },
@@ -140,7 +143,8 @@ var KeyboardAwareLegendList = typedForwardRef(function KeyboardAwareLegendList2(
       freeze,
       keyboardLiftBehavior,
       keyboardOffset,
-      onContentInsetChange
+      onContentInsetChange,
+      ScrollViewComponent
     ]
   );
   const AnimatedLegendListInternal = reanimated.AnimatedLegendList;
diff --git a/node_modules/@legendapp/list/keyboard.mjs b/node_modules/@legendapp/list/keyboard.mjs
index 87b38b9..8df951a 100644
--- a/node_modules/@legendapp/list/keyboard.mjs
+++ b/node_modules/@legendapp/list/keyboard.mjs
@@ -1,5 +1,6 @@
 import * as React from 'react';
 import { useRef, useEffect, useMemo, useCallback, useLayoutEffect } from 'react';
+import { Platform } from 'react-native';
 import { KeyboardChatScrollView, KeyboardController } from 'react-native-keyboard-controller';
 import { useSharedValue } from 'react-native-reanimated';
 import { internal } from '@legendapp/list/react-native';
@@ -69,6 +70,7 @@ var KeyboardAwareLegendList = typedForwardRef(function KeyboardAwareLegendList2(
     freeze,
     keyboardLiftBehavior,
     keyboardOffset,
+    ScrollViewComponent,
     ...rest
   } = props;
   const refLegendList = useRef(null);
@@ -108,7 +110,8 @@ var KeyboardAwareLegendList = typedForwardRef(function KeyboardAwareLegendList2(
           freeze,
           keyboardLiftBehavior,
           offset: keyboardOffset,
-          onContentInsetChange
+          onContentInsetChange: Platform.OS === "android" ? onContentInsetChange : void 0,
+          ScrollViewComponent
         }
       );
     },
@@ -119,7 +122,8 @@ var KeyboardAwareLegendList = typedForwardRef(function KeyboardAwareLegendList2(
       freeze,
       keyboardLiftBehavior,
       keyboardOffset,
-      onContentInsetChange
+      onContentInsetChange,
+      ScrollViewComponent
     ]
   );
   const AnimatedLegendListInternal = AnimatedLegendList;
